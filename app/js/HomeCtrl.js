'use strict';
angular.module('critical.controllers.homeCtrl', ['ngRoute'])
    .controller('HomeCtrl', HomeCtrl);

HomeCtrl.$inject = ['$scope', '$routeParams', '$injector'];

function HomeCtrl($scope, $routeParams, $injector) {
    /** modules **/
};